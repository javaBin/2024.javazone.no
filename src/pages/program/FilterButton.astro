---
import type {HTMLAttributes} from "astro/types";
interface Props extends HTMLAttributes<'button'> {
    dataActive?: boolean;
}
---
    <button {...Astro.props}
        class="bg-darkgreen py-6 px-6 sm:px-10 text-white no-underline rounded-md hover:bg-white border-white border-2 hover:border-darkgray hover:text-darkgray max-sm:text-lg whitespace-nowrap"
        data-active={Astro.props.dataActive}>
        <slot/>
    </button>


